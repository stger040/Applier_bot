# --- Core: browser automation (always required) ---
undetected-chromedriver>=3.5.0
selenium>=4.0.0
pyautogui>=0.9.54
setuptools>=65.0.0

# --- Web dashboard (app.py) ---
flask>=2.0.0
flask-cors>=4.0.0

# --- AI features (imported whenever use_AI = True in config/secrets.py) ---
# openai also covers DeepSeek and other OpenAI-compatible endpoints.
openai>=1.0.0
google-generativeai>=0.8.0

# --- Resume generator feature (modules/resumes/generator.py) ---
python-docx>=1.0.0
fpdf2>=2.7.0
